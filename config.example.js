/**
 * config.example.js — Template for local config
 * Copy this file to config.js and add your actual API keys.
 * config.js is in .gitignore so your keys will never be committed.
 */

window.HS_CONFIG = {
  GROQ_API_KEY: 'YOUR_GROQ_API_KEY_HERE'
};
